/**
 * Vendor imports
 */

import { ReactElement } from "react";

/**
 * Custom imports.
 */

/**
 * LoadBox functional component.
 */
interface Props {
  children: ReactElement;
}

export const LoadBox = function LoadBox({ children }: Props) {};
